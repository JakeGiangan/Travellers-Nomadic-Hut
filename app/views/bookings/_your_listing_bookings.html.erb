<div class="row">
    <div class="col">
        <div class="card" style="margin-top: 20px;">
            <div class="card-header">
                <h5 class="mb-0">Your Listing Bookings</h5>
            </div>
            <div class="card-body">
                <% @user_listings.each do |item|  %>
                    <div class="row" style="margin-bottom: 15px;">
                        <div class="col-xl-2 d-xl-flex align-items-xl-center">
                            <p><%= item.check_in_date.strftime("%B %d, %Y") %></p>
                        </div>
                        <div class="col-xl-2"><img /></div>
                        <div class="col d-xl-flex align-items-xl-center">
                            <p><%= item.check_in_date %><br /><%= item.first_name %> <%= item.last_name %></p>
                        </div>
                        <div class="col d-xl-flex align-items-xl-center"><button class="btn btn-primary col-lg-12" type="button">Review Guest</button></div>
                    </div>
                <% end %>
                <div class="row">
                    <div class="col d-xl-flex justify-content-xl-center" style="margin-top: 15px;">
                        <nav>
                            <%= will_paginate @user_listings %>
                        </nav>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>