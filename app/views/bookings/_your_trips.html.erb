<div class="row">
    <div class="col">
        <div class="card" style="margin-top: 20px;">
            <div class="card-header">
                <h5 class="mb-0">Your Booked Trips</h5>
            </div>
            <div class="card-body">
                <% @user_trips.each do |item| %>
                    <div class="row" style="margin-bottom: 15px;">
                        <div class="col-xl-2 d-xl-flex align-items-xl-center">
                            <p><%= item.check_in_date.strftime("%B %d, %Y") %></p>
                        </div>
                        <div class="col-xl-2"><img /></div>
                        <div class="col d-xl-flex align-items-xl-center">
                            <p><%= item.listing_name %><br /><%= item.first_name %> <%= item.last_name %></p>
                        </div>
                        <div class="col d-xl-flex align-items-xl-center">
                            <div id="postHostReview_<%=item.id%>" role="dialog" tabindex="-1" class="modal fade show">
                                <div class="modal-dialog" role="document">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h4 class="modal-title">Post a Review</h4><button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button></div>
                                        <%= form_for(@review) do |f| %>
                                            <div class="modal-body">
                                                <%= f.text_field :user_id, value: current_user.id %>
                                                <%= f.text_field :listing_id, value: item.id %>
                                                <%= f.text_area :content, class: "col-lg-12" %>
                                            </div>
                                            <div class="modal-footer">
                                                <button class="btn btn-light" type="button" data-dismiss="modal">Close</button>
                                                <%= f.submit "Save", class:"btn btn-primary" %>
                                            </div>
                                        <% end %>
                                    </div>
                                </div>
                            </div>
                            <button class="btn btn-primary col-lg-12 roundedButton" type="button" data-toggle="modal" data-target="#postHostReview_<%= item.id %>" >Review Host</button>
                        </div>
                    </div>
                <% end %>
                
                <div class="row">
                    <div class="col d-xl-flex justify-content-xl-center" style="margin-top: 15px;">
                        <nav>
                            <%= will_paginate @user_trips %>
                        </nav>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>